name: apidash_cli
description: CLI for API Dash — run, manage and test APIs from the terminal
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  args: ^2.6.0
  http: ^1.6.0
  path: ^1.9.0

executables:
  apidash: apidash
  ad: apidash
