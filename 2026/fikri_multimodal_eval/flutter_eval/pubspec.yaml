name: flutter_eval
description: "Multimodal AI Evaluation — Flutter frontend for lm-eval-harness, lmms-eval, inspect-ai, faster-whisper."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  dio: ^5.7.0
  fl_chart: ^0.70.2
  shared_preferences: ^2.3.3
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
