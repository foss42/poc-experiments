# I have implemented the Open Resources specification compliant pipeline and the A2UI framework along with GenUI sdk in Flutter.

## This shows how a simple prompt can be transformed into an interative UI component response, also users can view and copy/reuse this code.
